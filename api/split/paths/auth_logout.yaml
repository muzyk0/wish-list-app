post:
  description: Clear refresh token cookie and invalidate session
  responses:
    '200':
      description: Logout successful
      content:
        application/json:
          schema:
            $ref: >-
              ../components/schemas/wish-list_internal_domain_auth_delivery_http_dto.MessageResponse.yaml
  security:
    - BearerAuth: []
  summary: Logout user
  tags:
    - Authentication
