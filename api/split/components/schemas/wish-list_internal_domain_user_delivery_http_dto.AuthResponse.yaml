properties:
  accessToken:
    description: Access token (short-lived, 15 minutes)
    type: string
  refreshToken:
    description: Refresh token (long-lived, 7 days) - also set as httpOnly cookie
    type: string
  user:
    allOf:
      - $ref: ./wish-list_internal_domain_user_delivery_http_dto.UserResponse.yaml
    description: User information
required:
  - accessToken
  - refreshToken
  - user
type: object
